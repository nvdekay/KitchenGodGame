/** Users admin — placeholder. A future implementation would use
 *  `createAdminClient()` (service role) in a server component or route handler
 *  to list users, with its own authorization already guaranteed by the layout. */
export default function AdminUsersPage() {
  return (
    <section>
      <h1 className="text-2xl font-bold">Users</h1>
      <p className="mt-2 text-neutral-600">User management placeholder.</p>
    </section>
  );
}
